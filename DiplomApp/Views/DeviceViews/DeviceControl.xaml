<UserControl x:Class="ClientApp.DeviceViews.DeviceControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ClientApp.DeviceViews"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Button x:Name="deviceBtn" Style="{StaticResource BtnTemlpate}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <WrapPanel Grid.Column="0">
                <Label Content="Название устройства:"/>
                <Label/>
            </WrapPanel>
            <WrapPanel Grid.Column="1">
                <Label Content="Значение:"/>
                <Label/>
            </WrapPanel>
            <Button Grid.Column="3"
                    Content="Настройки"
                    Width="80" Height="25"
                    Click="Button_Click"/>
        </Grid>
    </Button>
</UserControl>
